<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finsight.infrastructure.mapper.DataQualityMapper">

    <select id="classificationCoverage" resultType="map">
        select
            count(*) as totalTxns,
            sum(case when coalesce(trim(consume_code), '') = ''
                      and coalesce(trim(consume_name), '') = '' then 1 else 0 end) as unclassifiedTxns,
            round(sum(case when coalesce(trim(consume_code), '') = ''
                      and coalesce(trim(consume_name), '') = '' then 1 else 0 end)
                  / nullif(count(*), 0) * 100, 2) as unclassifiedPct,
            round(sum(case when coalesce(trim(consume_code), '') = ''
                      and coalesce(trim(consume_name), '') = ''
                 then abs(coalesce(balance_money, 0) + coalesce(income_money, 0)) else 0 end), 2) as unclassifiedAmount
        from `transaction`
        where deleted is null or deleted = 0
    </select>

    <select id="countOrphanCategoryTransactions" resultType="int">
        select count(*)
        from `transaction` t
        left join cls_category c on c.code = t.consume_code
        where (t.deleted is null or t.deleted = 0)
          and coalesce(trim(t.consume_code), '') &lt;&gt; ''
          and (c.id is null or coalesce(c.deleted, 0) = 1)
    </select>

    <select id="countRefundExcluded" resultType="int">
        select count(*)
        from v_transaction_analytics v
        where v.is_refund = 1
    </select>

    <select id="merchantTokenCoverage" resultType="map">
        select
            count(*) as totalRows,
            sum(case when coalesce(trim(v.merchant_token), '') &lt;&gt; '' then 1 else 0 end) as withToken,
            round(sum(case when coalesce(trim(v.merchant_token), '') &lt;&gt; '' then 1 else 0 end)
                  / nullif(count(*), 0) * 100, 2) as tokenCoveragePct
        from v_transaction_analytics v
        where v.is_transfer = 0
    </select>
</mapper>
