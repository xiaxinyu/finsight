# v2.0.0 发布验收清单

## 功能冻结

- [ ] 无新业务报表 / Profile 维度 / AI 功能
- [ ] 变更范围符合 [v2.0.0-stability-quality-plan.zh-cn.md](../roadmap/v2.0.0-stability-quality-plan.zh-cn.md)

## 数据库

- [ ] Flyway V30 索引已在目标环境执行（`mvn spring-boot:run` 或手动 migrate）
- [ ] V22–V29 已应用（分类治理、rule stats 等）
- [ ] 手动 seed（如需要）：`docs/tech/database/l2-category-sprint2-seed.sql`

## 性能基线

- [ ] Profile warm cache P95 < 800ms
- [ ] Advisor warm cache P95 < 500ms
- [ ] Forecast GET P95 < 1s
- [ ] 连续刷新 Profile 20 次 MySQL CPU 无持续 >50% 平台
- [ ] GET 不写 `fin_profile_snapshot` / `fin_insight_card` / 无意义 `fin_forecast_run`

验证命令：

```bash
mvn test -Dtest=AnalyticsPerformanceSmokeTest,FinancialProfileServiceTest,ForecastServiceTest,RecommendationServiceTest
# 生产/预发（需凭证）：
# bash scripts/ops/analytics-p95-smoke.sh
```

## UI / 响应式

- [ ] Profile、Dashboard、Reports、Transactions Detail 在 360–1920px 无页面级横向溢出（**手工** DevTools 设备模式；Playwright 可选本地 `npm run test:e2e`，CI 不跑）
- [ ] Profile 三层信息架构：摘要 → 维度矩阵 → 紧凑 Advisor
- [ ] 移动端 Radar 显示维度条形列表

## CI

- [ ] `mvn test` 通过
- [ ] `mvn checkstyle:check` 通过
- [ ] `cd frontend && npm test && npm run build` 通过
- [ ] `cd frontend && npm run lint` 通过（0 errors）
- [ ] `cd frontend && bash scripts/check-bundle-size.sh` 通过（主包 ≤ 4.2MB）
- [ ] `bash scripts/check-doc-links.sh` 通过

## 文档

- [ ] [profile-performance-runbook.zh-cn.md](./profile-performance-runbook.zh-cn.md)
- [ ] [FEATURE_FLAGS.md](../FEATURE_FLAGS.md) 性能说明已更新
- [ ] [REPORT_UI.md](../REPORT_UI.md) 响应式规范已更新

## 回滚

1. 部署上一 GA tag（v1.9.0）
2. V30 索引无需回滚
3. 清理异常 forecast run（可选）：

```sql
DELETE FROM fin_forecast_line WHERE run_id IN (
  SELECT id FROM fin_forecast_run WHERE id LIKE 'preview-%'
);
```

## 签署

| 项 | Owner | 证据 |
|----|-------|------|
| 性能 smoke | | CI / 日志 |
| Flyway | | migrate 日志 |
| UI 截图 | | 手工 DevTools 视口抽查 |
| 文档 | | PR link |
