# v2.0.2 发布验收清单

范围：[v2.0.2-professional-finance-quality-plan.zh-cn.md](../roadmap/v2.0.2-professional-finance-quality-plan.zh-cn.md)

## P0：语义层与 Profile 物化

- [ ] Flyway V32（`v_transaction_finance_semantics`）已应用
- [ ] Flyway V33（`fin_profile_current`）已应用
- [ ] [finance-semantic-contract.zh-cn.md](../finance/finance-semantic-contract.zh-cn.md) 与 V32 一致
- [ ] GET `/api/v1/analytics/profile` 不触发 `computeProfile()`
- [ ] POST `/api/v1/analytics/profile/refresh` 写入 `fin_profile_current` 且并发保护有效
- [ ] 数据变更后 Profile 标记 `stale`，不自动阻塞 GET
- [ ] Expanded semantic metrics（REAL_INCOME、CONSUMPTION_EXPENSE 等）写入 `fin_metric_monthly`
- [ ] `FinanceSemanticMetricsTest` 通过
- [ ] Dashboard KPI 使用语义 period-summary（Real income / Consumption / Net）
- [ ] Transactions 语义筛选与编辑区口径说明
- [ ] `personal-finance-reporting-guide.zh-cn.md` 与 release checklist 更新

验证命令：

```bash
mvn test -Dtest=FinancialProfileServiceTest,ProfileMaterializationApiTest,FinanceSemanticMetricsTest,PeriodMetricsServiceTest,AnalyticsMetricsControllerTest
cd frontend && npm test -- --run src/utils/dashboardMetrics.test.ts src/utils/transactionSemantic.test.ts
mvn clean package -DskipTests
cd frontend && npm test && npm run build
bash scripts/check-doc-links.sh
```

## 性能（目标）

- [ ] profile-read P95 < 200ms（物化命中路径）
- [ ] profile-refresh P95 < 3s
- [ ] 可选预发：`bash scripts/ops/analytics-p95-smoke.sh`

## CI

- [ ] `mvn test` 通过
- [ ] `mvn checkstyle:check` 通过
- [ ] `cd frontend && npm test && npm run build` 通过
- [ ] `bash scripts/check-doc-links.sh` 通过

## 回滚

1. 部署上一 GA tag
2. V32/V33 为 additive，通常无需回滚 DDL
3. 如需禁用物化读取，回滚应用层至 v2.0.1（GET 仍可能同步计算）

## 签署

| 项 | Owner | 证据 |
|----|-------|------|
| Flyway V32/V33 | | migrate 日志 |
| Profile materialization tests | | CI |
| Profile refresh UX | | 截图 / 手工 |
| Semantic contract | | PR link |
