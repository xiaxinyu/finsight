# v2.0.1 发布验收清单

对照 [v2.0.1-quality-optimization-plan.zh-cn.md](../roadmap/v2.0.1-quality-optimization-plan.zh-cn.md)。

## P0 性能

- [ ] `bash scripts/check-analytics-sql-gate.sh` 通过（CI `backend-sql-gate`）
- [ ] `bash scripts/ops/analytics-p95-smoke.sh`：Profile <800ms、Advisor <500ms、Forecast <1s、Trend <1.2s
- [ ] 连续刷新 Profile 30 次 `fin_profile_snapshot` 不增长（GET 只读）
- [ ] 交易/分类变更后 Profile/Advisor/Forecast 缓存失效（`MetricRefreshTrigger`）

## P0 算法

- [ ] Forecast 使用 `hybrid_projection`（rolling + 季节性 + 趋势）
- [ ] Backtest 使用 `cutoff_hybrid_projection`，输出 MAPE/MAE/bias/coverage
- [ ] Profile `overallScore` 权重表 + `confidence` + `sampleMonths`
- [ ] 样本不足时 backtest 返回 `insufficientSample`

## P1 桌面 UI / CI（不含移动端、不含 Playwright）

- [ ] 桌面端（≥1024px）Profile、Reports、Transactions 布局抽查
- [ ] CI：`mvn test`、checkstyle、`backend-sql-gate`、`backend-performance`、frontend test/lint/build/bundle、doc-links

## 命令

```bash
mvn test
mvn checkstyle:check
bash scripts/check-analytics-sql-gate.sh
cd frontend && npm test && npm run lint && npm run build
bash scripts/check-doc-links.sh
# 预发：
# bash scripts/ops/analytics-p95-smoke.sh
```

## 回滚

回滚至 v2.0.0 tag；V30 索引可保留；清理 preview forecast run 见 v2.0.0 release checklist。
